% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{tls_family}
\alias{tls_family}
\alias{binomial_tls}
\alias{beta_binomial_tls}
\alias{beta_tls}
\title{Response families for thermal-load-sensitivity models}
\usage{
binomial_tls()

beta_binomial_tls()

beta_tls()
}
\value{
A \code{tls_family} object: a list with \code{family}, \code{family_code}
(0 binomial, 1 beta-binomial, 2 beta), and \code{links} for the natural-scale
parameters.
}
\description{
\code{binomial_tls()} and \code{beta_binomial_tls()} describe the count response
distribution for \code{\link[=fit_tls]{fit_tls()}}, and \code{beta_tls()} the continuous-proportion
response in \verb{(0, 1)} (e.g. PSII operating efficiency or relative chlorophyll
fluorescence). All three model survival as a four-parameter logistic function
of log10 duration; the beta-binomial and beta families add a dispersion
parameter \code{phi}.
}
\details{
The \code{phi} convention for the beta-binomial family is the \strong{sum of the Beta
shape parameters}: for fitted survival probability \code{p}, counts are
Beta-Binomial with shapes \code{a = p * phi} and \code{b = (1 - p) * phi}. Larger \code{phi}
means \emph{less} overdispersion (the binomial is recovered as \code{phi} grows). This
matches the simulation convention in \code{\link[=simulate_tls]{simulate_tls()}} and differs from the
precision/size parameterisations used by some other packages. The \code{beta}
family uses the \strong{same shapes} for the continuous proportion, \code{y ~ Beta(p * phi, (1 - p) * phi)}, so \code{phi} carries the identical meaning and a
larger \code{phi} again means a tighter response around the fitted curve.
}
\examples{
binomial_tls()
beta_binomial_tls()
beta_tls()
}
