% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{get_z}
\alias{get_z}
\title{Extract the thermal-sensitivity (z) estimate(s)}
\usage{
get_z(fit, conf.int = TRUE, conf.level = 0.95, method = c("wald", "profile"))
}
\arguments{
\item{fit}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{conf.int}{Logical; include confidence-interval columns (default
\code{TRUE}).}

\item{conf.level}{Confidence level for the interval (default \code{0.95}).}

\item{method}{Either \code{"wald"} (default) or \code{"profile"}.}
}
\value{
A \link[tibble:tibble]{tibble} of the \code{z} row(s) from
\code{\link[=tidy_parameters]{tidy_parameters()}}.
}
\description{
Extract the thermal-sensitivity (z) estimate(s)
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 60)
get_z(fit)
}
