% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{profile_tls-methods}
\alias{profile_tls-methods}
\alias{print.profile_tls}
\alias{summary.profile_tls}
\alias{print.summary.profile_tls}
\alias{coef.profile_tls}
\alias{vcov.profile_tls}
\alias{logLik.profile_tls}
\alias{AIC.profile_tls}
\alias{nobs.profile_tls}
\title{S3 methods for fitted freqTLS models}
\usage{
\method{print}{profile_tls}(x, digits = 4, ...)

\method{summary}{profile_tls}(object, ...)

\method{print}{summary.profile_tls}(x, digits = 4, ...)

\method{coef}{profile_tls}(object, complete = FALSE, ...)

\method{vcov}{profile_tls}(object, ...)

\method{logLik}{profile_tls}(object, ...)

\method{AIC}{profile_tls}(object, ..., k = 2)

\method{nobs}{profile_tls}(object, ...)
}
\arguments{
\item{x}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{digits}{Number of significant digits for the estimates table.}

\item{...}{Ignored.}

\item{object}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{complete}{Logical; return the full estimates data frame instead of a
named vector.}

\item{k}{Penalty per parameter (default \code{2}, giving the AIC).}
}
\value{
\code{print} and \code{summary} return their input invisibly / the summary
object; the extractors return the quantities named in their titles.
}
\description{
Standard extractor and display methods for the \code{profile_tls} object returned
by \code{\link[=fit_tls]{fit_tls()}}: \code{print}, \code{summary}, \code{coef}, \code{vcov}, \code{logLik}, \code{AIC}, and
\code{nobs}. They mirror the drmTMB method idioms
(\verb{drmTMB::R/methods.R:2-40,1826-1864,2025-2037}).
}
\section{Functions}{
\itemize{
\item \code{print(profile_tls)}: Print a compact, readable summary: the call,
the family, \code{tref}, the data summary, the natural-scale estimates table,
and the convergence state.

\item \code{summary(profile_tls)}: Build a \code{summary.profile_tls} object carrying
the estimates table (with Wald z-statistics and p-values), the family,
\code{tref}, the data summary, and the convergence state.

\item \code{print(summary.profile_tls)}: Print a \code{summary.profile_tls} object.

\item \code{coef(profile_tls)}: Extract natural-scale point estimates. With
\code{complete = FALSE} (default) a named numeric vector; with \code{complete = TRUE}
the full \code{estimates} data frame (parameter, group, estimate, std.error).

\item \code{vcov(profile_tls)}: The variance-covariance matrix of the
internal (unconstrained) coordinates, from \code{\link[TMB:sdreport]{TMB::sdreport()}}. Returns
\code{NULL} (with a warning) when the \code{sdreport} did not produce a covariance.

\item \code{logLik(profile_tls)}: The maximised log-likelihood, as a \code{logLik}
object with \code{df} and \code{nobs} attributes so \code{\link[stats:AIC]{stats::AIC()}} and
\code{\link[stats:BIC]{stats::BIC()}} work.

\item \code{AIC(profile_tls)}: Akaike's An Information Criterion. With the
default \code{k = 2} this returns the stored \code{AIC}; other \code{k} are computed from
the log-likelihood and \code{df}.

\item \code{nobs(profile_tls)}: The number of observations (temperature-by-
duration cells) used in the fit.

}}
\keyword{internal}
