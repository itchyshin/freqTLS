% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tdt-utils.R
\name{compute_4pl_bounds}
\alias{compute_4pl_bounds}
\title{Derive 4PL asymptote intervals from a user-supplied response range}
\usage{
compute_4pl_bounds(lower = 0, upper = 1, pad = 0.001, gap = 0.002)
}
\arguments{
\item{lower, upper}{Numeric scalars. The response-scale range that the
asymptotes can occupy (\code{0} and \code{1} for proportion data;
\code{0.85} and \code{1} for PSII-like sublethal data, etc.).}

\item{pad}{Absolute padding from \code{lower} and \code{upper}. Default \code{0.001}.}

\item{gap}{Absolute gap between the low and up intervals. Default \code{0.002}.}
}
\value{
Named list with \code{low_min}, \code{low_max}, \code{low_w}, \code{up_min}, \code{up_max},
\code{up_w}, \code{midpoint}.
}
\description{
Given the lower and upper bounds of where the asymptotes can sit, returns
the disjoint intervals used by \code{\link[bayesTLS:make_4pl_formula]{make_4pl_formula()}}'s \code{inv_logit} reparam.
\code{low} is mapped to \verb{(lower + pad, midpoint - gap/2)}, \code{up} to
\verb{(midpoint + gap/2, upper - pad)}. The gap kills label-switching by ensuring
\code{up > low} always; the pad keeps the asymptotes off the exact boundaries.
}
\keyword{internal}
