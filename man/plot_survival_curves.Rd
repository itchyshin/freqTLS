% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_survival_curves}
\alias{plot_survival_curves}
\title{Plot fitted survival curves against duration}
\usage{
plot_survival_curves(fit, temps = NULL, times = NULL, ...)
}
\arguments{
\item{fit}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{temps}{Numeric vector of temperatures to draw curves for. Defaults to
the distinct observed temperatures (capped at a readable number).}

\item{times}{Numeric vector of durations to evaluate the smooth curve over.
Defaults to a log-spaced sequence over the observed duration range.}

\item{...}{Reserved; must be empty.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_survival_curves()} draws the fitted survival probability as a function
of exposure duration (on a log10 x-axis), one curve per temperature, with the
observed survival proportions overlaid as points. For a grouped fit the
curves are faceted by group.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
plot_survival_curves(fit)

}
