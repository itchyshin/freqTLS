% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_4pl.R
\name{make_4pl_formula}
\alias{make_4pl_formula}
\title{Build a freqTLS 4PL formula from the direct CTmax/z interface}
\usage{
make_4pl_formula(
  ctmax = NULL,
  z = NULL,
  up = NULL,
  low = NULL,
  k = NULL,
  by = NULL,
  family = "beta_binomial"
)
}
\arguments{
\item{ctmax, z, up, low, k}{One-sided formulas (or \code{NULL}). \code{ctmax}/\code{z} set the
CTmax / thermal-sensitivity structure and must have the same fixed-effect
model-matrix columns; \code{up}/\code{low}/\code{k} set the 4PL shape. Random intercepts
are supported on \code{ctmax}, \code{z}, \code{low}, and \code{k}, but not \code{up}.}

\item{by}{Optional single, non-missing moderator column name. It is shorthand
for \code{ctmax = z = ~ 0 + by} when those are not given explicitly; it does not
add random effects or modify \code{low}, \code{up}, or \code{k}.}

\item{family}{\code{"beta_binomial"}, \code{"binomial"}, or \code{"beta"} (selects the
response idiom: \code{n_surv | trials(n_total)} for counts, bare \code{survival} for
the continuous-proportion beta family).}
}
\value{
A \code{tls_formula} object (as built by \code{\link[=tls_bf]{tls_bf()}}).
}
\description{
Translates the bayesTLS-style direct-mode arguments (\code{ctmax}, \code{z}, \code{up},
\code{low}, \code{k}, \code{by}) into the engine's \code{\link[=tls_bf]{tls_bf()}} \code{tls_formula} object. Supplying
a \code{ctmax} and/or \code{z} formula is the direct parameterisation; \code{by} is shorthand
for fixed cell means of CTmax and z by one moderator (\code{~ 0 + by}); it does
not add random effects or modify the shape formulas.
}
\details{
Following the freqTLS constant-shape invariant, the asymptotes and steepness
(\code{up}, \code{low}, \code{k}) default to \strong{shared} (\code{~ 1}) so the temperature effect runs
through the midpoint (CTmax / z) only; pass an explicit formula to let a shape
vary. \code{ctmax} and \code{z} must produce the same fixed-effect model-matrix columns.
Supported random intercepts go inside the \code{ctmax}/\code{z}/\code{low}/\code{k} formulas;
\code{up} random effects are not supported. For example,
\code{ctmax = ~ 1 + (1 | batch)} keeps the same intercept-only fixed design as the
default \code{z = ~ 1} while adding a \code{CTmax} random intercept.
}
\examples{
make_4pl_formula()
make_4pl_formula(by = "population", family = "binomial")
}
\seealso{
\code{\link[=fit_4pl]{fit_4pl()}}, \code{\link[=tls_bf]{tls_bf()}}, \code{\link[=standardize_data]{standardize_data()}}
}
