% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{check_tls}
\alias{check_tls}
\title{Report identifiability diagnostics for a fitted model}
\usage{
check_tls(fit)
}
\arguments{
\item{fit}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}, or a \code{freq_tls} workflow from
\code{\link[=fit_4pl]{fit_4pl()}}.}
}
\value{
Invisibly, a character vector of the diagnostic codes that fired.
}
\description{
\code{check_tls()} re-runs the data-adequacy diagnostics (SPEC.md S10, items 1-8)
on a fitted \code{profile_tls} object, including the two post-fit checks that
\code{\link[=fit_tls]{fit_tls()}} cannot run before the model exists: whether any fitted \code{CTmax} is
extrapolated beyond the assayed temperatures (item 7), and whether \code{phi} has
reached the binomial limit (item 8). Each concern is emitted as a
\code{\link[cli:cli_warn]{cli::cli_warn()}}. Use it to audit a fit, or after \code{\link[=suppressWarnings]{suppressWarnings()}} around
\code{\link[=fit_tls]{fit_tls()}}.
}
\details{
The profile-geometry diagnostics (items 9-12) are emitted by \code{\link[=confint]{confint()}} and
\code{\link[=profile]{profile()}} when those are called, not here, because they require the profile
likelihood.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
check_tls(fit)
}
