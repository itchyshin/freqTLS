% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{derive_tcrit}
\alias{derive_tcrit}
\title{Derive the critical temperature at a damage-rate floor (T_crit)}
\usage{
derive_tcrit(object, rate = 1, group = NULL)
}
\arguments{
\item{object}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{rate}{Damage-rate floor(s), a percentage of the lethal dose per hour
(strictly positive). A scalar or a vector; default \code{1}.}

\item{group}{Optional single group level (grouped fits only; required when the
fit is grouped).}
}
\value{
A numeric vector of critical temperatures (degrees C), one per \code{rate}.
}
\description{
\code{derive_tcrit()} returns the rate-multiplier critical temperature \code{T_crit}: the
temperature at which the thermal-damage rate falls to a chosen low floor
\code{rate}. It is the maximum-likelihood analogue of the \code{bayesTLS}
\code{extract_tdt()} absolute-family \code{T_crit}, and follows directly from the fitted
\code{CTmax} and \code{z}:
\deqn{T_{crit} = CTmax + z \, \log_{10}(rate / 100).}
Because \code{rate < 100} makes \code{log10(rate / 100) < 0} and \code{z > 0}, \code{T_crit} sits
below \code{CTmax}: it is the lower thermal threshold at which damage becomes
negligible (the temperature cutoff a heat-injury accumulation model treats as
"no damage").
}
\details{
\code{rate} is a damage-rate floor expressed as a \strong{percentage of the lethal dose
per hour}; \code{bayesTLS} brackets observed breakpoints with a default range of
\code{0.1}--\code{1} \%/hour. Unlike the Bayesian path, which samples \code{rate} to fold an
operational choice into the posterior, freqTLS treats \code{rate} as a fixed
input and returns the deterministic transform of the fitted \code{CTmax} and \code{z}
(combine their confidence intervals if you need to propagate uncertainty).

\code{T_crit} assumes a \strong{lethal endpoint}: it is a damage-accumulation concept, so
for sublethal endpoints (knockdown, photosynthetic failure) the steeper \code{z}
drives it implausibly low. \code{derive_tcrit()} says so, once per call.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
derive_tcrit(fit, rate = c(0.1, 1)) # lower thermal thresholds
}
\seealso{
\code{\link[=derive_ctmax]{derive_ctmax()}} for the absolute-threshold critical temperature.
}
