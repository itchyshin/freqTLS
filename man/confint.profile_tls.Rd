% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{confint.profile_tls}
\alias{confint.profile_tls}
\title{Confidence intervals for a fitted thermal-load-sensitivity model}
\usage{
\method{confint}{profile_tls}(
  object,
  parm = NULL,
  level = 0.95,
  method = c("profile", "wald", "bootstrap"),
  npoints = 30L,
  trace = FALSE,
  fallback = TRUE,
  nboot = 1000L,
  boot_seed = NULL,
  cores = 1L,
  ...
)
}
\arguments{
\item{object}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{parm}{Character vector of target names (for example \code{"CTmax"}, \code{"z"},
\code{"log_z"}, \code{"low"}, \code{"k"}, \code{"phi"}, grouped names such as \code{"CTmax:A"}, or
contrasts such as \code{"dCTmax:A-B"}). \code{NULL} (default) returns intervals for the
natural-scale parameters of the fit.}

\item{level}{Confidence level (default \code{0.95}).}

\item{method}{One of \code{"profile"} (default), \code{"wald"}, or \code{"bootstrap"}.}

\item{npoints}{Number of grid points used per profile (default \code{30}); ignored
for \code{method = "wald"} and \code{method = "bootstrap"}.}

\item{trace}{Logical; print inner-optimisation progress (profile/bootstrap).}

\item{fallback}{Logical; when \code{method = "profile"} (the default), fall back to
the parametric bootstrap for any parameter whose profile does not close, and
for all parameters when the fit's Hessian is not positive definite
(\code{pdHess = FALSE}). Default \code{TRUE}.}

\item{nboot}{Number of bootstrap replicates for \code{method = "bootstrap"} or the
fallback (default \code{1000}).}

\item{boot_seed}{Optional integer seed making the bootstrap reproducible
without disturbing the caller's random stream (default \code{NULL}).}

\item{cores}{Number of CPU cores for the bootstrap refits (default \code{1}).
\code{cores > 1} refits replicates in parallel by forking (Unix; sequential on
Windows). Results are identical for a given \code{boot_seed} regardless of \code{cores}.}

\item{...}{Reserved; must be empty.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per target and columns
\code{parameter}, \code{conf.low}, \code{conf.high}, \code{estimate}, \code{level}, \code{method},
\code{scale}, and \code{conf.status}.
}
\description{
\code{confint()} returns confidence intervals for the natural-scale parameters of a
\code{\link[=fit_tls]{fit_tls()}} model. Three methods are available:
}
\details{
\itemize{
\item \code{method = "profile"} (default) computes profile-likelihood confidence
intervals by inverting the likelihood-ratio test: the interval is
\code{{psi : D(psi) <= qt(1 - alpha/2, df)^2}}, found by \code{\link[stats:uniroot]{stats::uniroot()}} on each
side of the MLE on the unconstrained internal coordinate, with the endpoints
transformed to the natural scale. The cutoff is the squared profile-t
quantile on \code{df = n - p} residual degrees of freedom (Bates-Watts profile-t),
not \code{qchisq(level, 1)}; the two coincide as \code{df -> Inf}. These intervals are
prior-free and respect asymmetry. They are equivariant under monotone
reparameterisation, so the \code{z} interval equals \code{exp()} of the internal
\code{log_z} interval.
\item \code{method = "wald"} reuses the Phase-2 Wald path: \verb{estimate +/- t * se} (with
\code{t = qt(1 - alpha/2, df)} on \code{df = n - p}) on the internal (link) scale,
back-transformed.
\item \code{method = "bootstrap"} returns prior-free parametric-bootstrap percentile
intervals: survival counts are regenerated at the observed design from the
fitted 4PL, the model is refitted \code{nboot} times, and the interval is the
percentile range of the replicate estimates. This is the likelihood-path
analogue of the bayesTLS posterior interval, and returns a finite interval
whenever the estimator is stable.
}

When a profile does not close on one side, or the fitted Hessian is not
positive definite (\code{pdHess = FALSE}), \code{confint()} falls back to the parametric
bootstrap for the affected parameters (with a message) so an interval is still
returned -- the parity with a Bayesian fit, which always yields one. Set
\code{fallback = FALSE} to keep the strict profile behaviour, which returns \code{NA} on
the open side (never a fabricated bound) with a warning that the parameter is
weakly identified (SPEC.md S10, R-PROFILE). The upper asymptote \code{up} has its own
coordinate \code{beta_up} under disjoint bounds but is not yet profiled, so it is
reported with the delta-method Wald interval under the profile/Wald methods,
with a message.

For a fit with a random intercept (\verb{CTmax ~ <fixed> + (1 | group)}),
\code{method = "profile"} profiles the fixed-effect coordinates by re-running the
Laplace approximation at each grid point, which is slower than a fixed-effects
profile. \code{sigma_CTmax} keeps its log-scale Wald interval, the non-closing
fallback uses Wald (the bootstrap does not yet carry the random block), and
\code{method = "bootstrap"} returns the Wald intervals with a message.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
confint(fit, "CTmax", method = "profile")
confint(fit, "z", method = "profile")

}
