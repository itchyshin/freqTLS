% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tdt-utils.R
\name{tdt_resolve_time_multiplier}
\alias{tdt_resolve_time_multiplier}
\title{Resolve the model-to-output time multiplier for TDT helpers}
\usage{
tdt_resolve_time_multiplier(time_multiplier, meta, output_time_unit)
}
\arguments{
\item{time_multiplier}{Numeric scalar or \code{NULL}.}

\item{meta}{A \code{bayes_tls} workflow's \code{meta} list (uses \code{duration_unit}).}

\item{output_time_unit}{Target output time unit (e.g. \code{"min"}).}
}
\value{
Numeric scalar multiplier.
}
\description{
If \code{time_multiplier} is supplied it is returned unchanged (explicit override).
Otherwise it is derived from the workflow's \code{meta$duration_unit} (the unit of
the model's \code{duration} column) and the requested \code{output_time_unit}, so that
\code{model_time * time_multiplier} is in \code{output_time_unit}. Falls back to \code{1}
(with a message) when the units cannot be resolved.
}
\keyword{internal}
