% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_stage.R
\name{ts_stage1}
\alias{ts_stage1}
\title{Stage 1 of the classical two-stage TDT pipeline}
\usage{
ts_stage1(
  data,
  temp = "temp",
  duration = "duration",
  n_surv = "n_surv",
  n_total = "n_total",
  family = c("binomial", "betabinomial")
)
}
\arguments{
\item{data}{Data frame with one row per (temperature, duration) replicate.}

\item{temp, duration, n_surv, n_total}{Column names (strings) for assay
temperature (°C), exposure duration, survivors, and trials.}

\item{family}{\code{"binomial"} or \code{"betabinomial"}.}
}
\value{
A tibble with one row per temperature: \code{temp}, \code{log10_lt50},
\code{se_log10_lt50}, \code{slope}, \code{phi} (beta-binomial precision, else \code{NA}),
\code{finite_ok}, \code{bracket_ok}, \code{stage1_ok}.
}
\description{
Fits a separate logistic dose-response curve at each assay temperature and
reads off \code{log10(LT50)} (the duration at 50\% survival). The binomial family
uses \link[stats:glm]{stats::glm}; the beta-binomial family uses
\link[glmmTMB:glmmTMB]{glmmTMB::glmmTMB} with a \code{betabinomial} family (overdispersion at Stage 1).
}
\details{
Two validity flags are returned so callers can choose their own success rule:
\code{finite_ok} (finite coefficients, negative non-trivial slope) and \code{bracket_ok}
(the fitted LT50 lies within the observed duration range, padded by 0.5 on the
log10 scale). \code{stage1_ok} is their conjunction.
}
\examples{
d <- data.frame(
  temp = rep(c(30, 34, 38), each = 12),
  dur  = rep(rep(c(1, 5, 15, 45), 3), times = 3),
  surv = rbinom(36, 20, 0.5), tot = 20)
ts_stage1(d, "temp", "dur", "surv", "tot", family = "binomial")
}
