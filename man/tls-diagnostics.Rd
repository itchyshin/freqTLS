% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{tls-diagnostics}
\alias{tls-diagnostics}
\title{Identifiability and data-adequacy diagnostics for thermal-load-sensitivity fits}
\description{
freqTLS emits explicit identifiability warnings rather than letting weak
data quietly produce confident-looking estimates. This is the package's
clearest value-add over the Bayesian path. The diagnostic contract splits into
two groups:
}
\details{
\itemize{
\item \strong{Data-adequacy (1-8)} depend only on the data and design and are checked by
\code{check_tls_data()}, which \code{\link[=fit_tls]{fit_tls()}} runs before optimising. They are also
surfaced after the fact by \code{\link[=check_tls]{check_tls()}}.
\item \strong{Profile-geometry (9-12)} depend on the profile likelihood and are emitted
by the profiling code in \code{R/profile.R} / \code{R/confint.R}.
}

All warnings use \code{\link[cli:cli_warn]{cli::cli_warn()}} so they can be caught with
\code{\link[=withCallingHandlers]{withCallingHandlers()}} / \code{\link[=tryCatch]{tryCatch()}} and silenced with
\code{\link[=suppressWarnings]{suppressWarnings()}}.
}
\keyword{internal}
