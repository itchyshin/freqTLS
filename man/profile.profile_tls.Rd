% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{profile.profile_tls}
\alias{profile.profile_tls}
\alias{print.profile_tls_profile}
\title{Profile-likelihood curves for a fitted thermal-load-sensitivity model}
\usage{
\method{profile}{profile_tls}(fitted, parm, level = 0.95, npoints = 30L, trace = FALSE, ...)

\method{print}{profile_tls_profile}(x, digits = 4, ...)
}
\arguments{
\item{fitted}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{parm}{A single target name (see Targets).}

\item{level}{Confidence level for the interval and the cutoff line (default
\code{0.95}).}

\item{npoints}{Number of grid points for the deviance curve (default \code{30}).}

\item{trace}{Logical; print inner-optimisation progress.}

\item{...}{Reserved; must be empty.}

\item{x}{A \code{"profile_tls_profile"} object.}

\item{digits}{Number of significant digits for the printed summary (default
\code{4}).}
}
\value{
An object of class \code{"profile_tls_profile"}: a list with \code{parm},
\code{profile_value} (grid on the natural scale), \code{deviance}, \code{estimate},
\code{conf.low}, \code{conf.high}, \code{conf.status}, \code{cutoff}, \code{level}, \code{scale}, and
\code{transformation}.
}
\description{
\code{profile()} computes the profile-likelihood deviance curve for one scalar
target of a \code{\link[=fit_tls]{fit_tls()}} model. For the target it fixes the corresponding
internal (unconstrained) coordinate on a grid, re-optimises the remaining
coordinates at each grid point, and returns the deviance
\code{D = 2 * (logLik_hat - logLik_profile)} together with the profile-t cutoff and
the profile-likelihood confidence interval. Because the profile is taken on the
unconstrained coordinate and the endpoints are then transformed by a monotone
function, the interval is exactly equivariant: the \code{z} interval equals \code{exp()}
of the internal \code{log_z} interval (the headline equivariance check).
}
\details{
The algorithm is a map-refit profile: the target coordinate is fixed with
TMB's \code{map} mechanism and the rest re-optimised, mirroring the bracket-then-
\code{\link[stats:uniroot]{stats::uniroot()}} endpoint solver in \code{drmTMB::R/profile.R:2314-2373}. See
\code{vignette("profile-likelihood")}.
}
\section{Functions}{
\itemize{
\item \code{print(profile_tls_profile)}: Print a compact summary of the profile.

}}
\section{Targets}{

\tabular{lll}{
\strong{Target} \tab \strong{Profiled coordinate} \tab \strong{Endpoint transform} \cr
\code{CTmax}, \verb{CTmax:<grp>} \tab \code{beta_CT[g]} \tab identity \cr
\code{z}, \verb{z:<grp>} \tab \code{beta_logz[g]} \tab \code{exp} \cr
\code{log_z}, \verb{log_z:<grp>} \tab \code{beta_logz[g]} \tab identity \cr
\code{low} \tab \code{beta_low} \tab \code{plogis} \cr
\code{k} \tab \code{beta_logk} \tab \code{exp} \cr
\code{phi} \tab \code{log_phi} \tab \code{exp} \cr
\code{up} \tab (Wald/delta fallback) \tab -- \cr
\verb{dCTmax:<a>-<b>}, \verb{dlog_z:<a>-<b>} \tab contrast recoding \tab identity \cr
}

Under the disjoint-bounds parameterisation \code{up = up_min + up_w * plogis(beta_up)}
has its own coordinate \code{beta_up}, but freqTLS does not yet profile it (the profile
path is wired for \code{low} but not \code{up} — symmetric work, simply not implemented).
freqTLS falls back to the delta-method Wald interval for \code{up}
and says so. Group contrasts (\code{dCTmax}, \code{dlog_z}) are profiled
directly by recoding the design so the contrast is itself a coordinate.
}

\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
pc <- profile(fit, "CTmax")
pc$conf.low
pc$conf.high

}
