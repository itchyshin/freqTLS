% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_survival_surface}
\alias{plot_survival_surface}
\title{Plot the fitted survival surface over temperature and duration}
\usage{
plot_survival_surface(fit, temps = NULL, times = NULL, contour = TRUE, ...)
}
\arguments{
\item{fit}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{temps, times}{Numeric grids passed to \code{\link[=predict_survival_surface]{predict_survival_surface()}}.
Defaults span the observed ranges.}

\item{contour}{Logical; overlay contour lines (default \code{TRUE}).}

\item{...}{Reserved; must be empty.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_survival_surface()} draws the fitted survival probability as a filled
heatmap over a temperature-by-duration grid, with contour lines, using
\code{\link[=predict_survival_surface]{predict_survival_surface()}}. Duration is on a log10 axis. For a grouped fit
the surface is faceted by group.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
plot_survival_surface(fit)

}
