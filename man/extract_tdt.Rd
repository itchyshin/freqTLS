% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_tdt.R
\name{extract_tdt}
\alias{extract_tdt}
\title{Extract z, CTmax and (optionally) T_crit with bootstrap confidence intervals}
\usage{
extract_tdt(
  object,
  target_surv = "relative",
  lethal = FALSE,
  TC_rate_range = c(0.1, 1),
  nboot = 1000L,
  level = 0.95,
  seed = NULL,
  by = NULL
)
}
\arguments{
\item{object}{A \code{freq_tls} fit from \code{\link[=fit_4pl]{fit_4pl()}} (or a \code{profile_tls} fit).}

\item{target_surv}{\code{"relative"} (curve midpoint, default), \code{"absolute"}
(50\% survival), or a numeric survival level in \verb{(0, 1)} for an LTx CTmax.
An absolute target must lie strictly between the fitted asymptotes for every
reported group; otherwise no finite crossing exists.}

\item{lethal}{If \code{TRUE}, also derive \code{T_crit} (the damage-rate-floor critical
temperature, \code{CTmax + z * log10(rate / 100)}, \code{rate} log-uniform over
\code{TC_rate_range}), anchored at the fit's reference time.}

\item{TC_rate_range}{Damage-rate floor range (percent of lethal dose per hour)
for \code{T_crit}. Default \code{c(0.1, 1)}.}

\item{nboot}{Number of bootstrap replicates (default 1000; smaller is faster).}

\item{level}{Confidence level (default 0.95).}

\item{seed}{Optional RNG seed for reproducible replicates / rate draws.}

\item{by}{Optional name for the grouping column; defaults to the fit moderator.}
}
\value{
A list with \verb{$z}, \verb{$CTmax}, (\verb{$T_crit} when \code{lethal}), and \verb{$meta}.
Each quantity is \verb{list(draws = <tibble>, summary = <tibble>)}. Column names
follow bayesTLS: \code{z_median/z_lower/z_upper} for z; \code{temp_median/temp_lower/ temp_upper} for CTmax and T_crit; per-draw value columns are \code{z} / \code{temp}.
\verb{$meta$tref} records the reference time; \verb{$meta$duration_unit} is retained
when the input is a \code{\link[=fit_4pl]{fit_4pl()}} workflow.
}
\description{
The frequentist analogue of \code{bayesTLS::extract_tdt()}. Runs a parametric bootstrap
(via the freqTLS engine), derives the thermal-death-time quantities on each
replicate, and returns the same nested \verb{$z} / \verb{$CTmax} / \verb{$T_crit} structure
(each a list of \code{draws} + \code{summary}). Each row of a per-replicate table is a
derived estimate from one parametric-bootstrap refit: it is a resampled
frequentist estimate, not a posterior draw. \verb{*_median} is the
maximum-likelihood point estimate and \verb{*_lower} / \verb{*_upper} are bootstrap
percentiles.
}
\examples{
\donttest{
raw <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
dat <- standardize_data(
  raw, temp = "temp", duration = "duration",
  n_total = "total", n_surv = "survived"
)
fit <- fit_4pl(dat, family = "binomial", t_ref = 1, quiet = TRUE)
tdt <- extract_tdt(fit, nboot = 10, seed = 1)
tdt$CTmax$summary
}
}
\seealso{
\code{\link[=fit_4pl]{fit_4pl()}}, \code{\link[=tls]{tls()}}, \code{\link[=get_z_summary]{get_z_summary()}}, \code{\link[=get_ctmax_summary]{get_ctmax_summary()}}
}
