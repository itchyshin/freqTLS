% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_stage.R
\name{ts_ci}
\alias{ts_ci}
\title{Uncertainty for the classical two-stage TDT quantities}
\usage{
ts_ci(
  stage2,
  method = c("delta", "mvn"),
  level = 0.95,
  t_ref = 60,
  time_multiplier = 1,
  TC_rate_range = c(0.1, 1),
  temp_grid = NULL,
  n_sim = 1000,
  seed = 123
)
}
\arguments{
\item{stage2}{Output of \code{\link[=ts_stage2]{ts_stage2()}}.}

\item{method}{\code{"delta"} or \code{"mvn"}.}

\item{level}{Confidence level. Default 0.95.}

\item{t_ref, time_multiplier, TC_rate_range}{As in \code{\link[=ts_stage2]{ts_stage2()}}.}

\item{temp_grid}{Temperatures for the line band (\code{"mvn"} only).}

\item{n_sim}{MVN draws (\code{"mvn"} only). Default 1000.}

\item{seed}{RNG seed (\code{"mvn"} only). Default 123.}
}
\value{
For \code{"delta"}, a list with \code{z} and \code{CTmax_1hr}, each
\code{list(point, lower, upper, lower_t, upper_t, se)}, plus \code{df_resid}.
For \code{"mvn"}, a list with \code{summary_ci} (z/CTmax/T_crit bounds) and
\code{curve_ci} (per-\code{temp_grid} line band).
}
\description{
Two propagation methods on the Stage-2 fit:
\itemize{
\item \code{"delta"} — delta-method standard errors for \code{z} and \code{CTmax}, with
\strong{both Normal and t quantiles} (the t-quantile is the small-sample
correction for the few Stage-2 residual degrees of freedom). This is the
method the bias simulation reports.
\item \code{"mvn"} — slope-CI inversion for \code{z} (defined only when the slope CI
is wholly negative) plus MVN simulation of the Stage-2 coefficients for
\code{CTmax} and \code{T_crit}, and a \code{predict.lm} confidence band for the LT-vs-T
line over \code{temp_grid}. This is the method the case studies report.
}
}
\examples{
d <- data.frame(
  temp = rep(c(30, 32, 34, 36, 38), each = 12),
  dur  = rep(c(1, 5, 15, 45, 135, 405), times = 10),
  surv = rbinom(60, 20, 0.4), tot = 20)
s2 <- ts_stage2(ts_stage1(d, "temp", "dur", "surv", "tot"))
ts_ci(s2, method = "delta")$z
}
