% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heat_injury.R
\name{heat_injury_envelope}
\alias{heat_injury_envelope}
\title{Parametric-bootstrap confidence envelope for a heat-injury trajectory}
\usage{
heat_injury_envelope(
  object,
  trace,
  group = NULL,
  target_surv = NULL,
  t_c = NULL,
  repair = NULL,
  irreversible = TRUE,
  nboot = 1000L,
  conf.level = 0.95,
  seed = NULL
)
}
\arguments{
\item{object}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}, or a \code{freq_tls} workflow
from \code{\link[=fit_4pl]{fit_4pl()}}.}

\item{trace}{A data frame with numeric columns \code{time} (strictly increasing,
at least two rows) and \code{temp} (degrees C).}

\item{group}{Optional single group level (grouped fits only; required when the
fit is grouped).}

\item{target_surv}{Optional absolute survival threshold defining one lethal
dose: a single probability strictly between the fitted lower and upper
asymptotes. \code{NULL} (default) uses the project-default relative threshold (the
curve midpoint \code{(low + up) / 2}), matching \code{\link[=derive_ctmax]{derive_ctmax()}} and \code{\link[=derive_lt]{derive_lt()}}.
For a bootstrap envelope the target must also be attainable in every
converged bootstrap refit; otherwise the function aborts rather than clipping
an invalid refit's threshold.}

\item{t_c}{Optional damage-cutoff temperature (degrees C): at or below it the
damage rate is zero. \code{NULL} (default) applies no cutoff.}

\item{repair}{Optional named list of Sharpe-Schoolfield repair parameters (see
Details); \code{NULL} (default) means no repair.}

\item{irreversible}{Logical; if \code{TRUE} (default) survival is monotone
non-increasing (mortality does not reverse even if dose is repaired).}

\item{nboot}{Number of bootstrap replicates (default \code{1000}).}

\item{conf.level}{Width of the pointwise confidence band (default \code{0.95}).}

\item{seed}{Optional integer seed; when supplied the bootstrap is reproducible
without disturbing the caller's random stream.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{time}, \code{temp}, \code{survival} (the
point-estimate trajectory from \code{\link[=predict_heat_injury]{predict_heat_injury()}}), and \code{conf.low} /
\code{conf.high} (the pointwise parametric-bootstrap confidence band).
}
\description{
\code{heat_injury_envelope()} is the uncertainty counterpart of
\code{\link[=predict_heat_injury]{predict_heat_injury()}}: it redraws the fitted curve parameters by parametric
bootstrap (the same machinery \code{\link[=confint]{confint()}} uses), re-integrates the survival
trajectory under the temperature \code{trace} for each draw with the documented
dose-accumulation map, and returns a pointwise confidence band around the
point-estimate survival curve. The band is \strong{prior-free} -- it carries no
prior and makes no probability statement about the parameters; it is the
likelihood-path analogue of the \code{bayesTLS} posterior survival band, \strong{not} a
credible band.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 60)
trace <- data.frame(time = seq(0, 2, by = 0.1),
                    temp = 34 + 6 * sin(seq(0, 2, by = 0.1)))
heat_injury_envelope(fit, trace, nboot = 50, seed = 1)
}
\seealso{
\code{\link[=predict_heat_injury]{predict_heat_injury()}} for the point trajectory, \code{\link[=plot_heat_injury]{plot_heat_injury()}}
to draw the band.
}
