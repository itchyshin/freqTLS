% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{tls_bf}
\alias{tls_bf}
\title{Build a freqTLS formula object (brms/drmTMB-style)}
\usage{
tls_bf(...)
}
\arguments{
\item{...}{The response-and-axes formula first (unnamed, with a left-hand
side), then sub-parameter formulas keyed by their left-hand side. See the
grammar above.}
}
\value{
A \code{tls_formula} object: a list with the captured response formula,
the named sub-parameter formulas, and the calling environment.
}
\description{
\code{tls_bf()} captures the per-sub-parameter formulas that define a \code{freqTLS}
model and returns them, unevaluated, as a \code{tls_formula} object. It is the
formula complement to the column interface of \code{\link[=fit_tls]{fit_tls()}}: instead of passing
bare column names, you write one response formula plus a formula per model
sub-parameter.
}
\details{
The \strong{first} argument must be the unnamed response-and-axes formula. Its
left-hand side names the survival counts, in either the brms idiom
\code{successes | trials(total)} or the \code{glm} idiom \code{cbind(successes, failures)}.
Its right-hand side names the two thermal-load-sensitivity axes with the
tagged markers \verb{time(<duration>)} and \verb{temp(<temperature>)} (order does not
matter).

The remaining arguments are sub-parameter formulas keyed by their left-hand
side, one of \code{low}, \code{up}, \code{log_k}, \code{CTmax}, or \code{log_z}. Any sub-parameter you
omit defaults to \code{~ 1}. Each of \code{low}, \code{up}, and \code{log_k} may carry its
\strong{own} design independently — a grouping factor (\code{low ~ group}), a
continuous covariate (\code{log_k ~ body_size}), or an intercept — and need not
share one factor or match the headline-parameter grouping. \code{CTmax} and \code{log_z}
accept fixed-effect formulas but must produce the same model-matrix columns
(for example, use \verb{CTmax ~ group, log_z ~ group}); their supported random-
intercept groupings may differ. A single random intercept,
\verb{<param> ~ <fixed> + (1 | group)}, is accepted on \code{CTmax}, \code{log_z}, \code{low}, and
\code{log_k} (one grouping factor each, intercept only) -- but not on the upper
asymptote \code{up}, for which the compiled objective has no random-intercept term. Putting the same
grouping factor on two or more of them fits independent variances (no
correlation term) and warns.
}
\section{Parser provenance}{

The shape of the parser (variadic capture via \code{\link[=substitute]{substitute()}}, a per-entry
formula walk, and random-bar detection) is adapted from drmTMB's
\code{drm_formula()} / \code{parse_drm_formula_entry()} (GPL-3); see \code{inst/COPYRIGHTS}.
freqTLS writes its own grammar (the \code{time()} / \code{temp()} axis markers, the
five fixed sub-parameter handles, and the package's supported random-effect
grammar).
}

\examples{
tls_bf(
  survived | trials(total) ~ time(duration) + temp(temp),
  CTmax ~ life_stage,
  log_z ~ life_stage
)
# cbind() response idiom, ungrouped:
tls_bf(cbind(survived, died) ~ time(duration) + temp(temp))

}
\seealso{
\code{\link[=fit_tls]{fit_tls()}}, which accepts either a \code{tls_formula} or the column
interface.
}
