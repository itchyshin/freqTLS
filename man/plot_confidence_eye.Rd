% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_confidence_eye}
\alias{plot_confidence_eye}
\title{Confidence-Eye (or line) display of headline confidence intervals}
\usage{
plot_confidence_eye(
  fit,
  parm = c("CTmax", "z"),
  method = c("profile", "wald", "bootstrap"),
  level = 0.95,
  style = c("eye", "line"),
  raw_data = FALSE,
  annotate = FALSE,
  legend = FALSE,
  fallback = TRUE,
  nboot = 1000L,
  boot_seed = NULL,
  cores = 1L,
  ...
)
}
\arguments{
\item{fit}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{parm}{Character vector of target parameter names. Defaults to
\code{c("CTmax", "z")}. Grouped names (e.g.
\code{"CTmax:larva"}) are accepted.}

\item{method}{One of \code{"profile"} (default), \code{"wald"}, or \code{"bootstrap"};
forwarded to \code{\link[=confint.profile_tls]{confint.profile_tls()}}.}

\item{level}{Confidence level (default \code{0.95}).}

\item{style}{One of \code{"eye"} (default; pale horizontal lens + hollow point) or
\code{"line"} (a confidence-interval bar with caps + hollow point, no lens).}

\item{raw_data}{Logical; overlay observed assay temperatures as a rug on
temperature-scale rows (default \code{FALSE}).}

\item{annotate}{Logical; add the plot title (default \code{FALSE}). Status
subtitles for open profiles and random-effects Wald routing are always
retained.}

\item{legend}{Logical; show the interval-status legend (default \code{FALSE}).}

\item{fallback, nboot, boot_seed, cores}{Forwarded to \code{\link[=confint.profile_tls]{confint.profile_tls()}}:
control the parametric-bootstrap fallback for non-closing profiles (so the
eye draws an honest bootstrap lens instead of only a hollow point), make it
reproducible, and parallelise the refits. Defaults \code{TRUE}, \code{1000}, \code{NULL},
and \code{1}.}

\item{...}{Reserved; must be empty.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_confidence_eye()} draws the freqTLS Confidence Eye for one or more
parameters (\code{CTmax}, \code{z}, or any other \code{\link[=confint.profile_tls]{confint.profile_tls()}}
target, including grouped names). It is a horizontal forest display: each
parameter (and group level) is a row, the parameter value runs along the
x-axis, and the confidence interval is a short, wide pale lens with a
hollow point estimate. The shallow horizontal lens reads as a confidence
\emph{interval}. The layout follows the gllvmTMB / drmTMB
Confidence-Eye contract.
}
\details{
\subsection{Honest fallback for open profiles}{

When a profile does not close (\code{conf.status} is \code{"open_lower"},
\code{"open_upper"}, \code{"open_both"}, or a bound is \code{NA}), no lens is drawn for that
row: a hollow point marks the estimate and the subtitle flags the open
interval. The eye is never fabricated from an open profile. A
\code{"wald_fallback"} interval (e.g. \code{up}) still gets a lens, with the source
noted in the caption.
}

\subsection{Raw data}{

With \code{raw_data = TRUE}, the observed assay temperatures are drawn as a rug
beneath a temperature-scale row (\code{CTmax}). This optional diagnostic shows the
data support and flags extrapolation when \code{CTmax} sits outside the assayed
range; it is off by default because assay temperatures do not support \code{z}.

Parameters on different scales (temperature for \code{CTmax}, a positive
multiplier for \code{z}) are stacked in separate panels with a free x-axis.
}
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 60)
plot_confidence_eye(fit, parm = c("CTmax", "z"))

}
