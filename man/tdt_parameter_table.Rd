% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tls.R
\name{tdt_parameter_table}
\alias{tdt_parameter_table}
\title{4PL parameter table (frequentist analogue of \code{tdt_parameter_table})}
\usage{
tdt_parameter_table(object, method = NULL, level = 0.95)
}
\arguments{
\item{object}{A \code{freq_tls} fit from \code{\link[=fit_4pl]{fit_4pl()}} (or a \code{profile_tls} fit).}

\item{method}{Interval method: \code{"wald"} (default) or \code{"profile"}.}

\item{level}{Confidence level (default 0.95).}
}
\value{
A tibble with \code{parameter}, \code{group}, \code{median}, \code{lower}, \code{upper}.
}
\description{
Returns the fitted 4PL parameters (\code{low}, \code{up}, \code{k}, \code{CTmax}, \code{z}, and \code{phi}
for over-dispersed families) as point estimates with confidence intervals, in
bayesTLS's \verb{parameter / [group] / median / lower / upper} shape. \code{low} and
\code{up} are the fitted survival asymptotes, \code{k} is curve steepness, \code{CTmax} is
the critical thermal maximum at the reference time, \code{z} is thermal sensitivity
in degrees per order-of-magnitude change in duration, and \code{phi} is
beta/beta-binomial precision
or overdispersion. The \code{median} name is retained for table compatibility; for
frequentist Wald or profile output it contains the maximum-likelihood point
estimate, not a posterior median.
}
\examples{
raw <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
dat <- standardize_data(
  raw, temp = "temp", duration = "duration",
  n_total = "total", n_surv = "survived"
)
fit <- fit_4pl(dat, family = "binomial", t_ref = 60, quiet = TRUE)
tdt_parameter_table(fit, method = "wald")
}
\seealso{
\code{\link[=tls]{tls()}}, \code{\link[=tidy_parameters]{tidy_parameters()}}
}
