% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_survival_curves.R
\name{predict_survival_curves}
\alias{predict_survival_curves}
\title{Predict the fitted survival surface with bootstrap confidence bands}
\usage{
predict_survival_curves(
  object,
  temps = NULL,
  durations = NULL,
  nboot = 500L,
  level = 0.95,
  seed = NULL,
  by = NULL
)
}
\arguments{
\item{object}{A \code{freq_tls} fit from \code{\link[=fit_4pl]{fit_4pl()}} (or a \code{profile_tls} fit).}

\item{temps}{Temperatures to predict at (default: the observed assay temps).}

\item{durations}{Exposure durations (default: 100 points log-spaced over the
observed range, in the data's duration unit).}

\item{nboot}{Number of bootstrap replicates for the bands (default 500).}

\item{level}{Confidence level (default 0.95).}

\item{seed}{Optional RNG seed.}

\item{by}{Optional name for the grouping column.}
}
\value{
A \code{freq_surv_curves} object: \verb{$summary} (a tibble of
\verb{[<group>,] temp, duration, survival_lower, survival_median, survival_upper})
and \verb{$meta}.
}
\description{
The frequentist analogue of \code{bayesTLS::predict_survival_curves()}. Evaluates the
fitted 4PL survival probability over a temperature-by-duration grid and adds
parametric-bootstrap confidence bands. For random-effects fits the curves are
population-level: random intercepts are integrated during bootstrap refits,
but no fitted group BLUP is added to the reported curve.
}
\examples{
\donttest{
raw <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
dat <- standardize_data(
  raw, temp = "temp", duration = "duration",
  n_total = "total", n_surv = "survived"
)
fit <- fit_4pl(dat, family = "binomial", t_ref = 1, quiet = TRUE)
curves <- predict_survival_curves(
  fit, temps = c(34, 36), durations = c(1, 4), nboot = 10, seed = 1
)
curves$summary
}
}
\seealso{
\code{\link[=fit_4pl]{fit_4pl()}}, \code{\link[=predict_survival_surface]{predict_survival_surface()}}, \code{\link[=tls]{tls()}}
}
