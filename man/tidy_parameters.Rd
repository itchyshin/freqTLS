% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{tidy_parameters}
\alias{tidy_parameters}
\title{Tidy the parameters of a fitted freqTLS model}
\usage{
tidy_parameters(
  fit,
  conf.int = TRUE,
  conf.level = 0.95,
  method = c("wald", "profile")
)
}
\arguments{
\item{fit}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{conf.int}{Logical; include \code{conf.low} / \code{conf.high} columns (default
\code{TRUE}).}

\item{conf.level}{Confidence level for the interval (default \code{0.95}).}

\item{method}{Either \code{"wald"} (default) or \code{"profile"}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per natural-scale parameter
and the columns \code{parameter}, \code{group}, \code{estimate}, \code{std.error},
\code{conf.low}, \code{conf.high}, \code{interval_type}, and \code{scale}. \code{scale} is the link
on which the interval was constructed (\code{"identity"}, \code{"log"}, or
\code{"logit"}); \code{interval_type} is \code{"wald"} or \code{"profile"}.
}
\description{
\code{tidy_parameters()} returns a broom-style tibble of the natural-scale
parameter estimates with optional Wald confidence intervals. The intervals
are computed on the internal (unconstrained / link) scale as
\verb{estimate +/- z * std.error} and then back-transformed to the natural scale,
so they respect each parameter's bounds (for example \code{z > 0}, \verb{0 < low < up})
and are equivariant under the link. For \code{CTmax} (identity link) this is the
usual symmetric Wald interval.
}
\details{
With \code{method = "profile"} the intervals are profile-likelihood confidence
intervals (see \code{\link[=confint.profile_tls]{confint.profile_tls()}}); with \code{method = "wald"} (default) they
are the back-transformed internal-link Wald intervals. The returned shape is
identical; only \code{interval_type} and the interval values differ. A profile that
does not close returns \code{NA} on the open side (never a fabricated bound).
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 60)
tidy_parameters(fit)
tidy_parameters(fit, method = "profile")

}
