% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_4pl.R
\name{fit_4pl}
\alias{fit_4pl}
\title{Fit the 4PL thermal-load-sensitivity model by maximum likelihood (TMB)}
\usage{
fit_4pl(
  data,
  ctmax = NULL,
  z = NULL,
  up = NULL,
  low = NULL,
  k = NULL,
  by = NULL,
  threshold = c("relative", "absolute"),
  p = 0.5,
  t_ref = 60,
  bounds = c(0, 1),
  family = NULL,
  method = c("profile", "wald", "bootstrap"),
  start = NULL,
  control = list(),
  trace = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{data}{Output of \code{\link[=standardize_data]{standardize_data()}}.}

\item{ctmax, z, up, low, k, by}{Direct-mode formula interface; see
\code{\link[bayesTLS:make_4pl_formula]{make_4pl_formula()}}. Supplying \code{ctmax}/\code{z} (or \code{by}) fits per-group CTmax/z.}

\item{threshold}{\code{"relative"} (default; CTmax/z at the curve midpoint) or
\code{"absolute"} (at the \code{p}-survival level). \emph{Absolute is wired into the
backbone in a later step; for now use \code{"relative"} and convert post hoc.}}

\item{p}{Survival level for the absolute threshold (default 0.5).}

\item{t_ref}{Reference exposure time (in the data's \code{duration_unit}) at which
CTmax is reported. Default 60 (e.g. minutes); use \code{t_ref = 1} for hours.}

\item{bounds}{Length-2 asymptote range \code{c(lower, upper)} (default \code{c(0, 1)}).}

\item{family}{\code{"beta_binomial"} (default for counts), \code{"binomial"}, or
\code{"beta"}. \code{NULL} picks beta for a proportion response, else beta-binomial.}

\item{method}{Default interval method for downstream extraction
(\code{"profile"}, \code{"wald"}, or \code{"bootstrap"}); stored in the object.}

\item{start, control, trace, quiet}{Passed to the engine \code{\link[=fit_tls]{fit_tls()}}.}
}
\value{
A \code{freq_tls} object: a list with \verb{$fit} (the engine fit), \verb{$data},
\verb{$formula}, and \verb{$meta} (threshold, t_ref, bounds, temp_mean, response_type,
family, grouped, moderators, method).
}
\description{
The frequentist twin of \code{bayesTLS::fit_4pl()}. Consumes \code{\link[=standardize_data]{standardize_data()}}
output and fits the single-stage 4PL thermal death-time model, parameterised
directly in CTmax and thermal sensitivity (z), via the freqTLS TMB engine.
Returns a \code{freq_tls} workflow object; uncertainty (Wald / profile / bootstrap)
is computed on demand by the quantity twins (\code{\link[bayesTLS:tls]{tls()}}, \code{confint()}).
}
\seealso{
\code{\link[=standardize_data]{standardize_data()}}, \code{\link[bayesTLS:make_4pl_formula]{make_4pl_formula()}}, \code{\link[=fit_tls]{fit_tls()}}
}
