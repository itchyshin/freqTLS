% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{dsuzukii_lethal}
\alias{dsuzukii_lethal}
\title{Drosophila suzukii lethal thermal-death-time assay, by sex}
\format{
A data frame with 94 rows and 5 columns:
\describe{
\item{temp}{Assay temperature in degrees Celsius. Five levels: 34, 35, 36,
37, 38 C.}
\item{time}{Exposure duration in \strong{minutes} -- note: minutes, not hours.
Set \code{tref} to a value on this scale (for example \code{tref = 240}, i.e. 4 h)
when fitting.}
\item{sex}{Sex, a factor with levels \code{F} (45 rows) and \code{M} (49 rows).}
\item{total}{Number of individuals in the cell (integer), the count of flies
at that \verb{(temp, time, sex)}.}
\item{survived}{Number surviving (integer), \code{total - n_dead} where \code{n_dead}
is the count of dead flies in the cell.}
}
}
\source{
Vendored from the \code{bayesTLS} package
(\url{https://github.com/daniel1noble/bayesTLS}), object \code{dsuzukii},
redistributed under CC BY 4.0. The primary deposit is Zenodo
\doi{10.5281/zenodo.10602268} (Ørsted, Hoffmann, Sgrò et al. 2024,
\emph{Global Change Biology}). The lethal-endpoint counts are aggregated to
\verb{(temp, time, sex)} cells (see \emph{Details} and
\code{data-raw/vendor_dsuzukii_lethal.R}).
}
\usage{
data(dsuzukii_lethal)
}
\description{
Survival counts from a lethal thermal-death-time (TDT) assay on the vinegar
fly \emph{Drosophila suzukii}, separated by sex. Individual flies were held at a
fixed assay temperature for a fixed exposure duration and scored alive or dead;
the raw \code{bayesTLS::dsuzukii} object is long (one row per fly), and this dataset
is the \strong{lethal endpoint aggregated to counts} per \verb{(temp, time, sex)} cell
(\verb{n_total = flies in the cell}, \verb{n_dead = number that died}), reshaped into the
\code{\link[=fit_tls]{fit_tls()}} column contract. It is grouped by \code{sex}, so a fit estimates a
separate \code{CTmax} and \code{z} per sex with shared \code{low}, \code{up}, and \code{k}.
}
\details{
Time is in \strong{minutes}. The original study (and the \code{bayesTLS} supplement)
summarise \code{CTmax} at a \strong{4-hour reference} (\code{tref = 240}) under the
\strong{absolute} LT50 threshold, following Ørsted et al. (2024); set \code{tref = 240}
(minutes = 4 h) when fitting to match those numbers.

The lethal counts are aggregated from the individual-level \code{bayesTLS::dsuzukii}
object exactly as in the \code{bayesTLS} supplement: \code{n_total = n()}, \code{n_dead = sum(dead)} grouped by \verb{(temp, time, sex)}, then \code{survived = n_total - n_dead}.
Each cell holds 9--24 flies (1407 flies and 622 deaths in total). The raw
object also carries the sublethal heat-coma (\code{t_coma}) and productivity
(\code{prod}) endpoints; those use time-to-event and hurdle models that are
freqTLS non-goals (see \code{docs/dev-log/known-limitations.md}), so only the
lethal-by-sex subset is vendored here. See \code{data-raw/vendor_dsuzukii_lethal.R}
for the aggregation script.
}
\section{Attribution}{

These data originate from the \code{bayesTLS} package by Daniel W. A. Noble,
Pieter A. Arnold and Patrice Pottier (2026, manuscript in preparation), which
introduced the thermal-load-sensitivity framework that freqTLS implements,
and from the original \emph{Drosophila suzukii} multi-trait thermal-tolerance assay
of Ørsted et al. (2024), deposited on Zenodo
(\doi{10.5281/zenodo.10602268}). They are redistributed under the Creative
Commons Attribution 4.0 International licence (CC BY 4.0). freqTLS code is
licensed GPL (>= 3); the CC BY 4.0 licence applies only to the vendored data.
Please cite \code{bayesTLS} (see \code{citation("freqTLS")}) when you use this
dataset.
}

\examples{
data(dsuzukii_lethal)
str(dsuzukii_lethal)
table(dsuzukii_lethal$sex)
# Fit a separate CTmax and z per sex (shared shape) at a 4-hour reference
# (time is in minutes, so tref = 240 = 4 h, matching Ørsted's absolute
# threshold). Wrap in suppressWarnings to swallow any data-adequacy notes.
fit <- suppressWarnings(
  fit_tls(dsuzukii_lethal, y = survived, n = total, time = time, temp = temp,
          group = sex, family = "beta_binomial", tref = 240)
)
fit$estimates[grepl("CTmax|^z", fit$estimates$parameter), ]
}
\keyword{datasets}
