% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tls.R
\name{tls}
\alias{tls}
\alias{tls_z}
\alias{tls_ctmax}
\alias{tls_tcrit}
\title{Thermal-load-sensitivity quantities (z, CTmax) with confidence intervals}
\usage{
tls(
  object,
  by = NULL,
  params = c("all", "z", "ctmax"),
  target_surv = "relative",
  lethal = FALSE,
  method = NULL,
  level = 0.95,
  nboot = 1000L,
  TC_rate_range = c(0.1, 1),
  seed = NULL
)

tls_z(object, ...)

tls_ctmax(object, ...)

tls_tcrit(object, ...)
}
\arguments{
\item{object}{A \code{freq_tls} fit from \code{\link[=fit_4pl]{fit_4pl()}} (or a bare \code{profile_tls} fit
from \code{\link[=fit_tls]{fit_tls()}}).}

\item{by}{Optional name for the grouping column in \verb{$summary}; defaults to the
fit's moderator (e.g. the \code{ctmax}/\code{z}/\code{by} factor).}

\item{params}{\code{"all"} (z and CTmax, the default), \code{"z"}, or \code{"ctmax"}.}

\item{target_surv}{Survival threshold for CTmax: \code{"relative"} (the curve
midpoint, the default), \code{"absolute"} (50\% survival), or a number in \verb{(0, 1)}
for a specific survival probability. Non-relative thresholds and \code{lethal} are derived per bootstrap
replicate via \code{\link[=extract_tdt]{extract_tdt()}}.}

\item{lethal}{If \code{TRUE}, also report \code{T_crit} (the damage-rate-floor critical
temperature); uses the bootstrap path.}

\item{method}{Interval method for the relative path: \code{"profile"} (default,
from the fit's stored default), \code{"wald"}, or \code{"bootstrap"}. Absolute /
\code{lethal} always use bootstrap.}

\item{level}{Confidence level (default 0.95).}

\item{nboot, TC_rate_range, seed}{Passed to \code{\link[=extract_tdt]{extract_tdt()}} for the bootstrap
path (absolute / LTx / \code{lethal}).}

\item{...}{Passed from \code{\link[=tls_z]{tls_z()}} / \code{\link[=tls_ctmax]{tls_ctmax()}} / \code{\link[=tls_tcrit]{tls_tcrit()}} to \code{\link[=tls]{tls()}}.}
}
\value{
A \code{tls} object: a list with \verb{$summary} (a tibble of
\verb{[<group>,] quantity, median, lower, upper}) and \verb{$meta}.
}
\description{
The frequentist analogue of \code{bayesTLS::tls()}. Reads a \code{\link[=fit_4pl]{fit_4pl()}} (\code{freq_tls})
fit and returns the thermal-load sensitivity quantities — thermal
sensitivity \code{z} and \code{CTmax} — as point estimates with confidence intervals,
one row per group when the fit is grouped. Uncertainty bounds are computed using
profile-likelihood intervals by default (or Wald / bootstrap via \code{method}).
}
\examples{
raw <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
dat <- standardize_data(
  raw, temp = "temp", duration = "duration",
  n_total = "total", n_surv = "survived"
)
fit <- fit_4pl(
  dat, family = "binomial", t_ref = 1, method = "wald", quiet = TRUE
)
tls(fit)
tls_z(fit)
tls_ctmax(fit)
}
\seealso{
\code{\link[=fit_4pl]{fit_4pl()}}, \code{\link[=tls_z]{tls_z()}}, \code{\link[=tls_ctmax]{tls_ctmax()}}, \code{\link[=confint.profile_tls]{confint.profile_tls()}}
}
