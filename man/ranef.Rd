% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{ranef}
\alias{ranef}
\alias{ranef.profile_tls}
\title{Random-effect BLUPs (conditional modes) for a freqTLS fit}
\usage{
ranef(object, ...)

\method{ranef}{profile_tls}(object, ...)
}
\arguments{
\item{object}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}} with a random intercept.}

\item{...}{Reserved; must be empty.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per group level (per RE term):
\code{group}, \code{term} (\code{"CTmax"}, \code{"log_z"}, \code{"low"}, or \code{"log_k"}), \code{estimate} (the
BLUP), and \code{std.error} (the conditional SE).
}
\description{
\code{ranef()} returns the predicted random intercepts (the conditional modes /
BLUPs) with their conditional standard errors, for a fit with a random
intercept on any of \code{CTmax}, \code{log_z}, \code{low}, or \code{log_k}
(\verb{<param> ~ <fixed> + (1 | group)}). It errors for a fixed-effects-only fit.
Each BLUP is a deviation on its coordinate's internal scale: \code{CTmax} in degrees
C, \code{log_z} on \code{log(z)}, \code{low} on \code{logit(low)}, \code{log_k} on \code{log(k)}. When several
REs are present the rows are stacked in \code{CTmax}, \code{log_z}, \code{low}, \code{log_k} order.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4,
                  re_sd = 1.5, n_re_groups = 12, seed = 42)
fit <- fit_tls(
  tls_bf(survived | trials(total) ~ time(duration) + temp(temp),
         CTmax ~ 1 + (1 | colony)),
  data = d, family = "binomial", tref = 1)
ranef(fit)
}
