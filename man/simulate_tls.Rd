% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_tls}
\alias{simulate_tls}
\title{Simulate survival-count data from the 4PL thermal-load-sensitivity model}
\usage{
simulate_tls(
  temps = seq(30, 42, by = 2),
  times = c(0.5, 1, 2, 4, 8),
  reps = 3,
  n = 20,
  low = 0.02,
  up = 0.98,
  k = 5,
  CTmax = 36,
  z = 3,
  phi = NULL,
  family = c("binomial", "beta_binomial", "beta"),
  group = NULL,
  re_sd = NULL,
  re_sd_z = NULL,
  re_sd_low = NULL,
  re_sd_logk = NULL,
  n_re_groups = NULL,
  re_group_name = "colony",
  tref = 1,
  seed = NULL
)
}
\arguments{
\item{temps}{Numeric vector of assay temperatures (degrees C).}

\item{times}{Numeric vector of exposure durations (native unit, e.g. hours).}

\item{reps}{Number of replicate observations per temperature-by-duration
cell (per group).}

\item{n}{Number of individuals per observation (binomial size).}

\item{low, up}{Lower and upper survival asymptotes (\verb{0 < low < up < 1}). A
scalar (shared) or, for a grouped simulation, one value per group.}

\item{k}{Steepness of the logistic on the \code{log10(duration)} scale (\code{k > 0}). A
scalar (shared) or, for a grouped simulation, one value per group.}

\item{CTmax}{Critical thermal maximum at \code{tref}. A scalar (ungrouped) or, for
a grouped simulation, a vector with one value per group.}

\item{z}{Thermal sensitivity (\code{z > 0}). A scalar or, when grouped, a vector
with one value per group.}

\item{phi}{Dispersion (sum of Beta shapes); \code{NULL} for the binomial family.
Required when \code{family = "beta_binomial"} or \code{family = "beta"}.}

\item{family}{One of \code{"binomial"}, \code{"beta_binomial"}, or \code{"beta"} (a
continuous proportion in \verb{(0, 1)}, returned in a \code{prop} column).}

\item{group}{Optional \strong{atomic} vector of group labels (character, factor,
or numeric). When supplied, \code{CTmax} and \code{z} must each be either a single
scalar (shared across groups) or a vector with one value per \emph{distinct}
group level, in the order the levels first appear. Passing a list (for
example \code{group = list(A = list(CTmax = 34))}) is an error: use the parallel
vector API instead, e.g.
\code{simulate_tls(group = c("A", "B"), CTmax = c(34, 38), z = c(3, 5))}.}

\item{re_sd}{Optional standard deviation of a \strong{random intercept on \code{CTmax}}.
When supplied (with \code{n_re_groups}), \code{n_re_groups} group-level deviations
\code{b_g ~ N(0, re_sd)} are drawn and each group's data is generated with
\code{CTmax_g = CTmax + b_g}. This is the data-generating analogue of the
\code{CTmax ~ 1 + (1 | group)} engine; \code{CTmax} and \code{z} must be scalars and it
cannot be combined with a fixed \code{group}. The realised deviations are
returned in \code{attr(, "truth")$b}.}

\item{re_sd_z}{Optional standard deviation of a \strong{random intercept on
\code{log(z)}}. When supplied (with \code{n_re_groups}), \code{n_re_groups} group-level
deviations \code{c_g ~ N(0, re_sd_z)} are drawn on the log scale and each group's
data is generated with \code{z_g = exp(log(z) + c_g)} (a multiplicative spread on
\code{z}). This is the data-generating analogue of the \code{log_z ~ 1 + (1 | group)}
engine. It may be combined with \code{re_sd} (both intercepts share the one
\code{re_group_name} grouping); the realised log-z deviations are returned in
\code{attr(, "truth")$b_logz}.}

\item{re_sd_low, re_sd_logk}{Optional standard deviations of \strong{random
intercepts on the lower asymptote \code{low} (logit scale)} and on the steepness
\strong{\code{log(k)}} — the shape-coordinate analogues of \code{re_sd} / \code{re_sd_z}
(\code{low_g = plogis(qlogis(low) + d_g)}, \code{k_g = exp(log(k) + e_g)}; \code{up} tracks
\code{low} by a fixed head-room fraction). They combine freely with \code{re_sd} /
\code{re_sd_z}; realised deviations are in \code{attr(, "truth")$b_low} / \verb{$b_logk}.}

\item{n_re_groups}{Number of random-effect groups (required with any \verb{re_sd*}).}

\item{re_group_name}{Name of the grouping column added to the output for the
random-effect mode (default \code{"colony"}).}

\item{tref}{Reference time at which \code{CTmax} is defined (default \code{1}).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A base \code{data.frame} with columns \code{temp}, \code{duration}, the true
probability \code{p}, and (when grouped) \code{group}. The count families
(\code{binomial}, \code{beta_binomial}) add \code{total} and \code{survived}; the \code{beta} family
instead adds a single continuous proportion column \code{prop}. The
data-generating parameters are attached as \code{attr(, "truth")}.
}
\description{
\code{simulate_tls()} draws survival counts from the locked data-generating
process used throughout the freqTLS test suite and benchmarks. It builds a
factorial grid of temperatures by durations by replicates, computes the true
survival probability under the direct-\code{CTmax}/\code{z} 4PL (the same forward map as
the TMB engine in \code{src/profile_tls.cpp}), and draws binomial or beta-binomial
counts. The simulating truth is attached as \code{attr(, "truth")}.
}
\details{
\subsection{The \code{phi} convention (R-PHI)}{

For the beta-binomial family, \code{phi} is the \strong{sum of the Beta shape
parameters}: counts are drawn as \code{prob <- rbeta(a = p * phi, b = (1 - p) * phi)} followed by \code{rbinom(n, prob)}. The Beta mean is \code{p} and its variance is
\code{p (1 - p) / (phi + 1)}, so \strong{larger \code{phi} means less overdispersion} and the
binomial is recovered as \code{phi -> Inf}. This matches the engine's
parameterisation in \code{\link[=beta_binomial_tls]{beta_binomial_tls()}}.
}
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
head(d)
attr(d, "truth")$CTmax

}
