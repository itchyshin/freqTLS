% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{derive_ctmax}
\alias{derive_ctmax}
\title{Derive the temperature giving a target survival at a fixed exposure}
\usage{
derive_ctmax(object, surv = NULL, duration = NULL, group = NULL)
}
\arguments{
\item{object}{A \code{profile_tls} fit from \code{\link[=fit_tls]{fit_tls()}}.}

\item{surv}{Target survival probability in \verb{(low, up)}. \code{NULL} (default) uses
the relative midpoint \code{(low + up) / 2}, reproducing \code{CTmax} at \code{tref}.}

\item{duration}{Exposure duration(s) (native time unit; strictly positive).
Defaults to the fit's \code{tref}.}

\item{group}{Optional single group level (grouped fits only).}
}
\value{
A numeric vector of temperatures (degrees C), one per \code{duration}.
}
\description{
\code{derive_ctmax()} inverts the fitted 4PL for \strong{temperature}: it returns the
assay temperature at which survival equals a target \code{surv} after exposure
\code{duration}. By default \code{surv} is the \emph{relative} midpoint threshold
\code{(low + up) / 2} and \code{duration} is \code{tref}, so \code{derive_ctmax(fit)} reproduces
the fitted \code{CTmax}. Supplying an \strong{absolute} \code{surv} gives the absolute-
threshold critical temperature (the analogue of the \code{bayesTLS}
\code{extract_tdt()} absolute mode), with the asymmetry correction
\code{qlogis((surv - low) / (up - low)) / k} built in.
}
\details{
Solving \code{surv = low + (up - low) * plogis(-k (log10(duration) - mid))} with
\code{mid = log10(tref) - (temp - CTmax) / z} for the temperature gives
\deqn{temp = CTmax - z\Big(\log_{10} duration - \log_{10} t_{ref} +
  \mathrm{qlogis}\!\big(\tfrac{surv - low}{up - low}\big) / k\Big).}
The target \code{surv} must lie strictly between \code{low} and \code{up}.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
derive_ctmax(fit)                                  # ~ CTmax (relative, at tref)
derive_ctmax(fit, surv = 0.5, duration = c(1, 4))  # absolute 50\% survival
}
