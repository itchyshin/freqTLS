% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_tls.R
\name{fit_tls}
\alias{fit_tls}
\title{Fit a single-stage 4PL thermal-load-sensitivity model by maximum likelihood}
\usage{
fit_tls(
  x,
  y,
  n,
  time,
  temp,
  group = NULL,
  family = c("beta_binomial", "binomial", "beta"),
  tref = 1,
  start = NULL,
  control = list(),
  trace = FALSE,
  quiet = FALSE,
  data = NULL
)
}
\arguments{
\item{x}{Either a data frame (column interface) or a \code{tls_formula} from
\code{\link[=tls_bf]{tls_bf()}} (formula interface). For back-compatibility the first argument is
still positional, so \code{fit_tls(my_data, y = survived, ...)} continues to
work.}

\item{y}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column of successes
(survivors), or, for the \code{beta} family, the response proportion in
\verb{(0, 1)}.}

\item{n}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column of trials (total
individuals). Required for the binomial and beta-binomial families; omit it
for the \code{beta} family, whose response is already a proportion.}

\item{time}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column of exposure
durations in the data's native unit (e.g. hours); used as
\code{log10(duration)} internally.}

\item{temp}{<\code{\link[rlang:args_data_masking]{data-masked}}> Column of assay
temperatures (degrees C).}

\item{group}{<\code{\link[rlang:args_data_masking]{data-masked}}> Optional grouping
column. When supplied, each group gets its own direct, profile-able \code{CTmax}
and \code{z} with shared \code{low}, \code{up}, and \code{k}. Defaults to \code{NULL} (a single
ungrouped fit).}

\item{family}{One of \code{"beta_binomial"} (default), \code{"binomial"}, or \code{"beta"}
(a continuous proportion in \verb{(0, 1)}), or a \code{tls_family} object from
\code{\link[=beta_binomial_tls]{beta_binomial_tls()}} / \code{\link[=binomial_tls]{binomial_tls()}} / \code{\link[=beta_tls]{beta_tls()}}.}

\item{tref}{Reference time at which \code{CTmax} is defined, in the same unit as
\code{time} (default \code{1}, i.e. CTmax at one time unit).}

\item{start}{Optional named list of starting values on the internal
(unconstrained) scale, overriding the defaults. Names must match the
parameters in \code{src/profile_tls.cpp} (\code{beta_low}, \code{beta_gap}, \code{beta_logk},
\code{beta_CT}, \code{beta_logz}, \code{log_phi}).}

\item{control}{List of optimiser controls; \code{optimizer} is passed to
\code{\link[stats:nlminb]{stats::nlminb()}}'s \code{control}, and \code{trace} toggles optimiser output.}

\item{trace}{Logical; print optimiser progress. A shortcut for
\code{control$trace}.}

\item{quiet}{Logical; if \code{TRUE}, suppress freqTLS's own data-adequacy and
identifiability diagnostic warnings and messages (the few-groups, beta
boundary-clamp, and same-grouping notes). Genuine errors and optimiser
warnings still surface, and \code{\link[=check_tls]{check_tls()}} reports the diagnostics on demand.
Default \code{FALSE}.}

\item{data}{Used only in the formula interface: the data frame the
\code{\link[=tls_bf]{tls_bf()}} columns are resolved against. Ignored in the column interface
(where the data frame is \code{x}).}
}
\value{
An object of class \code{c("profile_tls", "tls_fit")}: a list with the
call, the resolved \code{family}, \code{tref}, \code{group_levels}, a \code{data_summary}, the
internal-scale MLE \code{par}, an \code{estimates} data frame of natural-scale
parameters with standard errors, the \code{vcov} of the internal coordinates,
the \code{logLik}, residual \code{df}, \code{AIC}, a \code{convergence} list
(\code{code}/\code{pdHess}/\code{message}), the \code{name_map}, and the underlying TMB \code{obj},
optimiser \code{opt}, and \code{sdreport}.
}
\description{
\code{fit_tls()} fits the descending four-parameter logistic (4PL) thermal
death-time model to survival-count data, parameterised \strong{directly in \code{CTmax}
and \code{z}} (thermal sensitivity) so that both headline quantities can be
profiled. Survival is modelled as a function of \code{log10(duration)}; the
midpoint moves with temperature through \code{CTmax} and \code{z} (see
\code{vignette("model-math")} and \code{docs/design/01-model-and-parameterisation.md}).
}
\details{
There are two equivalent interfaces. In the \strong{column interface}, columns are
referenced with tidy evaluation: pass the bare column names of \code{data} (as in
\code{dplyr}), not strings. In the \strong{formula interface}, pass a \code{\link[=tls_bf]{tls_bf()}} object
as \code{x} and the data frame as \code{data}; the brms/drmTMB-style grammar names the
response, the two axes, and the \code{CTmax} / \code{log_z} predictors. Both interfaces
feed the same likelihood engine, so a grouped formula fit and the matching
\verb{group =} column fit are numerically identical.
}
\examples{
d <- simulate_tls(family = "binomial", CTmax = 36, z = 4, seed = 1)
fit <- fit_tls(d, y = survived, n = total, time = duration, temp = temp,
               family = "binomial", tref = 1)
fit$estimates

# The same fit through the formula interface:
fit2 <- fit_tls(
  tls_bf(survived | trials(total) ~ time(duration) + temp(temp)),
  data = d, family = "binomial", tref = 1
)

}
