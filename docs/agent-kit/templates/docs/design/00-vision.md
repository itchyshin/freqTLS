# <PROJECT> Vision

`<PROJECT>` exists to <plain-language purpose>.

## Readers And Users

The primary readers are:

- <applied users>;
- <method or algorithm developers>;
- <R package contributors>;
- <reviewers or maintainers>.

## Scope

The package supports:

- <workflow 1>;
- <workflow 2>;
- <workflow 3>.

The package does not currently support:

- <non-goal 1>;
- <non-goal 2>;
- <future idea that should not be described as implemented>.

## Core Contracts

Record the terms that must stay stable across code, docs, tutorials, and issue
threads.

| Contract | Meaning | Where Implemented | Validation |
| --- | --- | --- | --- |
| `<function>()` | <public workflow> | `<file>` | `<test or check>` |
| `<parameter>` | <scientific or computational meaning> | `<file>` | `<test or check>` |
| `<object field>` | <data or model contract> | `<file>` | `<test or check>` |

## Evidence Standard

An implemented claim needs:

- code path;
- test or simulation evidence;
- documentation or example;
- check-log entry;
- after-task or after-phase note for meaningful changes.

A planned claim should be labelled as planned in user-facing prose and roadmap
files.
