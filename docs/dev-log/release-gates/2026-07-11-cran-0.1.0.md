# freqTLS 0.1.0 CRAN release gates

Status date: 2026-07-12

## Ultra-plan routing and timing

The live Codex catalogue was checked with the bundled desktop runtime on
2026-07-11 and exposes `gpt-5.6-luna`, `gpt-5.6-terra`, and `gpt-5.6-sol`.
Model tier and reasoning effort are separate choices. This release uses the
house routing mnemonic: Luna for bounded scouting/mechanical inventory, Terra
as the default implementation tier, and Sol for orchestration and independent
release verification.

| Phase | Named owner | Model route | Work and output | Estimate |
| --- | --- | --- | --- | --- |
| Inventory and mechanical scans | Jason / Luna, low or medium | cheapest bounded scout | stale-claim scans, installed-file inventory, tarball listing, URL list | 10--20 min per pass |
| Package implementation | Emmy + Grace / Terra, high | default coder; higher effort for coupled R/docs/CI changes | CRAN mechanics, examples, documentation, tests, CI workflow | 45--90 min plus check runtime |
| Licensing and contract reconciliation | Rose / Sol, high | high-cost error and release-wide consistency | component ledger, exclusion proof, canonical/public scope audit | 20--40 min |
| Applied workflow gate | Pat / Terra, high | installed-user walkthrough | install, first fit, interval interpretation, warning recovery | 15--25 min |
| Final release gate | Ada + Grace + Rose / Sol, xhigh | independent orchestration and adversarial review | exact-tarball verdict and submission/no-submission decision | 20--30 min after platform results |

The initial implementation agents in this Codex session were not launched with
an explicit selectable model tier because the collaboration interface did not
expose a per-agent model parameter. Their outputs therefore count as producer
work, not as proof that the model-routing gate was followed. The final
independent verification must be run with the named Sol route or documented as
an unavailable deviation.

## Compute decision

No new simulation campaign is required for the current release slice: the
tested estimands and simulation evidence already exist, and CRAN readiness is
currently a packaging, licensing, documentation, and platform problem. Local
macOS runs the document/test/check/pkgdown loop; GitHub Actions covers Ubuntu R
release/devel, Windows release, and macOS release; win-builder R-devel and R-hub
Linux/clang check the exact tarball independently.

If a gate exposes a statistical-evidence gap, Curie and Gauss will first define
the estimand, data-generating process, parameter grid, repetitions, seed policy,
stopping rule, and acceptance thresholds. Totoro is the quick rehearsal host
with each worker single-threaded and conservative shared-machine usage; the
claim-bearing rerun moves to DRAC with scheduler, image/module, job-array, wall
time, memory, and output provenance recorded before launch. Totoro and DRAC
denominators must not be mixed silently.

## External consent gate

The submitted tarball may retain the `aut` roles for Pieter A. Arnold, Patrice
Pottier, and Daniel W. A. Noble only after the maintainer records confirmation
from each person. On 2026-07-12, Shinichi Nakagawa confirmed that all three
authors had agreed to proceed; the confirmations were received through email
and text messages. Private correspondence is not copied into the repository.

| Person | Required evidence | Status |
| --- | --- | --- |
| Pieter A. Arnold | Agreement to the `aut` role in freqTLS 0.1.0 | confirmed 2026-07-12 |
| Patrice Pottier | Agreement to the `aut` role in freqTLS 0.1.0 | confirmed 2026-07-12 |
| Daniel W. A. Noble | Agreement to the `aut` role in freqTLS 0.1.0 | confirmed 2026-07-12 |

The maintainer sent a group consent request on 2026-07-11 and subsequently
reported all three confirmations. This dated attestation supersedes the earlier
audit state in which a draft/request and the three replies were still pending.

## Data redistribution gate

Snow-gum material is CC BY-NC 4.0, not CC BY 4.0. The processed dataset, two
raw CSVs, and case-study source are preserved under the build-excluded
`data-raw/licensing-pending/snowgum/` tree. Snow-gum rows and metadata were
removed from the installed benchmark cache. The Kristineberg station extract,
which lacks explicit redistribution terms, is likewise preserved under
`data-raw/licensing-pending/kristineberg/`. Two unused environmental traces
derived from Open-Meteo/ERA5 and an Orsted/NicheMapR/NCEP workflow also lack a
complete recorded redistribution chain for every underlying provider; they are
preserved under `data-raw/licensing-pending/environmental-traces/` and have no
installed consumer. The package can proceed without these assets after the
final tarball inventory confirms their exclusion.

## Submission gate

Do not upload until the exact tarball passes the local, CI, win-builder, R-hub,
licensing-inventory, and Grace/Rose/Pat completion gates and all author-consent
rows above are closed. Do not describe the package as "on CRAN" until its public
CRAN package and check pages exist.

## Current local technical evidence

The current candidate is `freqTLS_0.1.0.tar.gz`, SHA-256
`1a8d1248a9517e2ba6df2cc595e181d3cc9846f52b868fdec61caac55326b331`,
1,551,284 bytes (1.48 MiB)
and 210 entries. On 2026-07-11 it passed `R CMD check --as-cran` under R 4.6.0
on aarch64 macOS with zero errors, zero warnings, and only the expected `New
submission` NOTE. Its inventory contains no licensing-pending data, snow-gum,
Kristineberg, excluded environmental traces, repository governance, build
scripts, or compiled artefacts. A clean installation and neutral-directory
render of the installed main vignette found the installed SVG and reproduced
the 69-text/27-rectangle/zero-`em` function map. At release commit `3fe45a9`,
GitHub Actions passed Ubuntu release/devel, Windows release, and macOS release
(run `29177778758`), and R-hub Ubuntu/clang passed (run `29177783632`). The
replacement win-builder R-devel run completed with one NOTE: `New submission`
plus spell-check flags for `TLS` and `reparameterised`; installation, examples,
tests, vignette rebuilding, and PDF/HTML manuals passed. Author confirmation
and external platform gates are closed. The final independent audit is recorded
below; CRAN submission and publication remain open.

## Fresh completion adversary

On 2026-07-12, three fresh reviewers audited the exact tarball and current
release records with a NOT-READY-by-default brief:

| Reviewer | Perspective | Verdict |
| --- | --- | --- |
| Grace | CRAN, platforms, artifact, and submission mechanics | READY |
| Rose | scope, licensing, provenance, consent, and stale claims | substantively READY; commit/push evidence batch and require a clean tree |
| Pat | clean installation and installed-user workflow | READY |

Rose found no substantive licensing, scope, provenance, consent, or claim
defect. Her process condition is resolved by the commit containing this table,
the updated current gate records, and the final after-task report; the pushed
branch must be clean before upload.
