# After-task: v0.2 polish and close-out

## Task goal

Close the v0.2 "do all" run: bring the public capability fully in sync and
refresh the stale internal status board, after the five v0.2 features and the
methodological vignette had landed.

## What changed

- `README.Rmd` / `README.md`: added a v0.2 capabilities bullet (random intercept
  on `CTmax` with profile intervals, grouped covariate effects on `low`/`up`/
  `log_k`, `derive_ctmax()` / `derive_tcrit()`, `predict_heat_injury()`) and a
  pointer to the new `vignette("frequentist-and-bayesian")`. Re-knit; the diff is
  prose-only (no figure churn).
- `docs/dev-log/dashboard/status.json`: refreshed the v0.1-era board -- `updated`
  2026-06-17, the `profileTLS` repo head/note (379 tests; the full v0.2 feature
  set; byte-identical gate held), and the stale "unsupported" matrix row (beta /
  random effects / grouped shapes are now implemented).

The capability matrix, known-limitations, ROADMAP, and NEWS were kept in sync in
each feature commit, so no further edits were needed here.

## Checks run and exact outcomes

- `python3 -m json.tool status.json` -> JSON OK (valid after edits).
- `devtools::build_readme()` -> OK; `git diff --stat README.md` = +8 lines
  (prose only).
- `pkgdown::check_pkgdown()` -> "No problems found" (the new functions
  `derive_tcrit`, `predict_heat_injury`, `beta_tls` and the new article are all
  in the index).
- The pkgdown site is rebuilt and deployed by CI on the next R-CMD-check success
  (the package itself was last checked 0/0/0 at the article commit; README and
  dashboard are doc-only and not part of the package build).

## v0.2 run summary (the whole "do all")

Shipped, each as a verified, committed, pushed slice on `feat/v0.1-core`:

| Slice | Commit |
| --- | --- |
| Beta (continuous-proportion) family | `b5c769c` |
| Profile-likelihood intervals under a random effect | `0d078ba` |
| `derive_tcrit()` rate-multiplier critical temperature | `f523645` |
| Deterministic `predict_heat_injury()` | `c55d761` |
| Shape-design engine refactor (byte-identical) | `bee8d04` |
| Grouped covariate effects on `low`/`up`/`log_k` | `5a9b78a` |
| Frequentist-vs-Bayesian vignette | `82b7c97` |
| v0.2 polish / close-out | (this commit) |

Tests: 309 at the start of the run -> **379 pass / 0 fail / 0 warn**. Every slice
was `R CMD check` (`_R_CHECK_FORCE_SUGGESTS_=false`) 0/0/0. The no-RE /
shared-shape / count byte-identical gate held through every `src/profile_tls.cpp`
change (the shape-design refactor was verified bit-identical for well-conditioned
fits via a `git stash` baseline comparison).

## Known limitations carried forward

- Profile / bootstrap intervals for grouped shape coordinates (Wald available).
- An RE-aware bootstrap for `sigma_CTmax` (Wald available).
- General continuous covariates on the shapes (grouped only).
- Absolute-target heat injury (relative threshold only).
- The maintainer-built bayesTLS + two-stage benchmark cache (needs Stan).
- CRAN hardening (a separate milestone).

## Next

The v0.2 roadmap is complete. Natural next steps if the maintainer wants them:
the deferred intervals above, a flagship grouped-shape / RE example in the
README, or beginning CRAN hardening.
