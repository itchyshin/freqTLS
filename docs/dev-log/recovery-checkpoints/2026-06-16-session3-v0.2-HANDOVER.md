# HANDOVER — profileTLS v0.2 sprint (read this first)

**For the next thread (start with zero context). Trust repo state + this file over any chat memory.**

## The standing goal (re-set with `/goal` in the new thread)
"Finish the whole thing — match the capabilities of and go **beyond** bayesTLS — surprise Dan genuinely — quick, accurate, better in many aspects."

**Framing is non-negotiable: COMPLEMENTARY / pluralistic. NO criticism of profileTLS OR bayesTLS.** Present speed/accuracy/coverage as descriptive characteristics of the likelihood path, never a scoreboard. The two packages are two valid lenses (likelihood + Bayesian). The flagship examples should be **random-effects models**, and the two approaches are compared **across all examples**.

## Where it stands
- **v0.1 is COMPLETE, verified, and LIVE.** TMB 4PL engine (binomial + beta-binomial), `fit_tls()` (column interface **and** `tls_bf()` brms/drmTMB-style formula interface), profile-likelihood CIs + 12 identifiability diagnostics, the **horizontal Confidence Eye** (forest layout, observed-temp rug = raw data; the maintainer's chosen default), `predict`/`predict_survival_surface`/`derive_lt`, `simulate_tls`, vendored `shrimp_lethal`/`zebrafish_lethal`, 4 vignettes, **217 tests / R CMD check 0-0-0**.
- **Live site:** https://itchyshin.github.io/profileTLS/ (served from the `gh-pages` branch; `tools/build-site.R` strips internal docs AGENTS/CLAUDE/SPEC before deploy). Repo homepage points to it.
- **Authorship:** Shinichi Nakagawa is **sole** author/maintainer. Noble, Arnold & Pottier are **acknowledged as the bayesTLS authors** (Description, README credit, bayesTLS citation entry) — **not** listed as profileTLS co-authors. Public shrimp-data wording is collegial ("counts reconstructed from source proportions"), no "bug" language. The pointed R-SHRIMP critique stays internal (SPEC.md / dev-log, excluded from the site).
- **Git:** branch `feat/v0.1-core`, PR #1 (https://github.com/itchyshin/profileTLS/pull/1), tip = this checkpoint's commit. `main` untouched.

## The key change THIS session: bayesTLS is now installed
- **`bayesTLS` 1.0.0 + cmdstanr (CmdStan 2.36.0) + brms + rstan are all installed.** The real bayesTLS head-to-head can finally run locally.
- **Real bayesTLS API (verified):** `fit_4pl(data, random_effects, temp_effects, lower, upper, family, prior, chains, iter, warmup, cores, seed, backend, control, init, file, file_refit, fit, ...)` — **no `t_ref`/`time_multiplier`**. Those live on `extract_tdt()` and `ts_stage2()`. `standardize_data()` and `ts_stage1/ts_stage2/ts_ci` exist.

## IMMEDIATE next steps (do these first, in order)
1. **Run the real head-to-head:** `Rscript data-raw/build_benchmark_cache.R` (now possible). It runs bayesTLS MCMC + two-stage on shrimp + each zebrafish stage and writes `inst/extdata/bayesTLS_benchmark_cache.rds`. ~minutes. The script was written from recon — **verify its `extract_tdt()`/`ts_ci()` column-name assumptions against bayesTLS 1.0.0 output and fix mismatches** (`summarise_tdt`/`pull_ci` are defensive but check).
2. **Fix the vignette recipe** `vignettes/comparing-to-bayesTLS.Rmd`: its `eval=FALSE` chunk passes `t_ref`/`time_multiplier` to `fit_4pl()` (WRONG) and skips `standardize_data()`. Correct it to the true workflow: `standardize_data() -> fit_4pl(temp_effects="mid", family=...) -> extract_tdt(t_ref=1, ...)`; `ts_stage1 -> ts_stage2(t_ref=1) -> ts_ci`. (Mirror the corrected `data-raw/build_benchmark_cache.R`, which is right.)
3. **Performance study — PARTIAL & UNTRACKED, verify:** `data-raw/performance-study.R` + `inst/extdata/performance_results.rds` exist from an **interrupted** Curie run. Re-run the script, sanity-check the speed / accuracy / coverage tables, then wire the article's "How fast / How accurate / How calibrated" sections to read the rds. Do NOT assume the partial output is correct.
4. **Rebuild + redeploy** `comparing-to-bayesTLS` with the REAL numbers (timing + point/CI + accuracy/coverage), complementary framing. Deploy via the gh-pages branch (the pkgdown workflow now does this; or run `tools/build-site.R` then push `pkgdown-site/` to `gh-pages`).

## The v0.2 capability roadmap (the goal)
1. **Random effects — APPROVED scope: random intercept on CTmax** (`CTmax ~ fixed + (1|group)`), matching bayesTLS's RE-on-mid. A detailed RE-1 spec is in task #20 / below.
   - **Engine (`src/profile_tls.cpp`):** add `DATA_IVECTOR(re_index)`, `DATA_INTEGER(n_re)`, `PARAMETER_VECTOR(b_CT)`, `PARAMETER(log_sd_CT)`; `CT_i += b_CT(re_index(i))` when `b_CT.size()>0`; `nll -= sum(dnorm(b_CT,0,exp(log_sd_CT),true))`; `MakeADFun(random="b_CT")`. **No-RE path MUST stay byte-identical** (n_re=0, b_CT length-0, log_sd_CT mapped `factor(NA)`, random=NULL → all 217 tests unchanged). This invariant is the gate.
   - Parse `CTmax ~ fixed + (1|group)` in `R/formula.R` (currently errors on `(1|...)`). RE on CTmax only; error on RE for other sub-params / random slopes / >1 grouping.
   - Add RE mode to `simulate_tls` (`re_sd`, `n_groups`). Add `tests/testthat/test-random-effects.R` (recover `sigma_CTmax`). Design doc `docs/design/08-random-effects.md`.
   - **RE Phase 2:** profiling/`confint` with RE (re-run Laplace at each profile point — the profile machinery must pass `random="b_CT"`), `ranef()`, methods, a CI for `sigma_CTmax`. Store `re_index`/`n_re`/`random` + BLUPs in the fit for this.
2. **Covariate effects on ALL sub-parameters** (`low`/`up`/`log_k` take design matrices, like CTmax/log_z) — matches bayesTLS `temp_effects = c("low","up","k","mid")`. Engine: add `X_low`/`X_up`/`X_logk` + beta vectors.
3. **Beta (continuous-proportion) family** — matches the snowgum/PSII case. New `family_code` in the cpp.
4. **Absolute thresholds + T_crit** in extract/derive — matches `extract_tdt` absolute mode (asymmetry correction `+ (1/k) log((up-p)/(p-low))`).
5. **Heat-injury / fluctuating-temperature survival prediction** — matches `predict_heat_injury` (Sharpe-Schoolfield repair). The big one; deterministic prediction from the fitted curve + a temperature trace (no Stan needed).
6. **Performance + complementary comparison** (speed/accuracy/coverage) on the RE examples.
7. **Capability matrix** (`docs/design/46-capability-matrix.md`), vignettes, site, NEWS/ROADMAP, final polish.

profileTLS's distinctive strengths to keep foregrounding (positively, not comparatively): profile-likelihood compatibility intervals, the 12 diagnostics, the Confidence Eye, ML speed, characterized coverage.

## Process discipline (carry over)
- **Verify every agent's load-bearing claims against the repo** — there were 3 partial/confabulated episodes this build (Ada wrote engine drafts out of scope + misreported; Boole's formula.R was interrupted mid-run; Curie's perf-study was interrupted). Always `git status` + re-run the gate; never trust the agent's summary alone.
- Named-perspective team in `.claude/agents/` (Ada/Gauss/Noether/Fisher/Emmy/Boole/Curie/Florence/Darwin/Pat/Jason/Grace/Rose) + job-function agents; phased builds with verification gates; dev-log after-task/check-log/recovery-checkpoints.
- Local checks first; CI = PR + workflow_dispatch, ubuntu-only, NO Stan in CI (cache + perf rds are committed artifacts).
- pkgdown excludes AGENTS/CLAUDE/SPEC via `tools/build-site.R`; keep it that way.

## Resume commands
```sh
cd "/Users/z3437171/Dropbox/Github Local/profileTLS"
git log --oneline -8 ; git status --short
sed -n '1,80p' docs/dev-log/recovery-checkpoints/2026-06-16-session3-v0.2-HANDOVER.md
R -q -e 'cat("bayesTLS:", requireNamespace("bayesTLS"), "| cmdstan:", as.character(cmdstanr::cmdstan_version()), "\n")'
R -q -e 'devtools::test(".")'              # expect 217 pass + skips
ls -la data-raw/performance-study.R inst/extdata/performance_results.rds  # untracked partials
```
