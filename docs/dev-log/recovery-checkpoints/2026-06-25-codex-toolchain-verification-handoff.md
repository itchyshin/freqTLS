# Handoff — Codex toolchain verification (2026-06-25)

**For:** Codex, or anyone on a working R + TMB toolchain (clean Linux R, or DRAC).
**Branch:** `main` (= `build/freqtls`) · **HEAD:** `d6cafa7` · **State:** clean,
both CI workflows green, site live at <https://itchyshin.github.io/freqTLS/>.

## Why this handoff exists

The whole 0.1.0 remediation arc — the audit fixes, the profile-t / disjoint-bounds
doc corrections, the authorship reorder (Noble last), the frequentist-trio headline,
the function map, and the Florence/Darwin/Rose/Pat findings — was done by Claude on a
Mac where **`library(TMB)` segfaults** (a TMB/Matrix ABI break in the local R). As a
result:

- every `man/*.Rd` was **hand-synced** to its roxygen, *not* regenerated by
  `devtools::document()`;
- `data-raw/build_benchmark_cache.R` gained a `dsuzukii` cell-aggregation step that
  was **never executed** (flagged in-file);
- verification was by `Rscript` on pure data/text (data shapes, regex logic, YAML,
  `parse()`) plus the ubuntu **R-CMD-check** workflow — which checks the *committed*
  man pages as-is and never runs `document()` to diff them against roxygen.

CI therefore does **not** cover: byte-exact `document()` regeneration,
`R CMD check --as-cran` extra checks, or the cache rebuild. This handoff closes
exactly that gap. Nothing here is expected to fail — it is confirmation, not repair.

## 0. Local toolchain: do NOT try to repair the dev Mac

**Root cause found (2026-06-26).** On the dev Mac the R **package library is
platform-mismatched**: `.libPaths()` resolves to an `x86_64-pc-linux-gnu-library/4.4`
tree (Linux, R 4.4) while R itself is **macOS arm64 R 4.6**. Every installed
`.so`/`.dylib` there (TMB included) is a Linux x86_64 build that macOS arm64 R
cannot `dyn.load` — that is the `*** caught segfault *** ... 'invalid permissions'`
at `dyn.load`. `install.packages("TMB", type = "source")` does **not** fix it (it
builds against the same mismatched library paths — the compile pulls
`.../x86_64-pc-linux-gnu-library/4.4/RcppEigen/include` under `clang -arch arm64`
— and fails). Repairing it means rebuilding the entire package library for macOS
arm64 R 4.6, which is out of scope for this work.

**So run §1–§4 on a clean toolchain** — DRAC, or any R whose library matches its
platform. Good news: the ubuntu **R-CMD-check** workflow already runs `test()` +
a full `R CMD check` **green** on a correct toolchain, so §2 and most of §3 are
already covered. The genuine residual is only the `document()` byte-exact diff
(§1) and the benchmark-cache rebuild (§4).

## 1. Regenerate the docs and diff

```r
devtools::document()
```
**Gate:** `git diff --stat man/` should be **empty** (the hand-sync was byte-exact)
or show only intended, reviewable changes. Commit any regenerated man pages. If a
page changes, the roxygen is the source of truth — keep the `document()` version and
note which file drifted (see §5).

## 2. Full test suite

```r
devtools::test()
```
**Gate:** all pass, including the two tripwires added this session:
- `tests/testthat/test-data-contract.R` — each dataset's name / rows / vars / key
  columns + a dev-only cross-check against the `R/data.R` `@format` text.
- `tests/testthat/test-doc-consistency.R` — package-title agreement across
  DESCRIPTION / `R/freqTLS-package.R` / `man/freqTLS-package.Rd` / `inst/CITATION`,
  and no present-tense "nested gap has no single coordinate" claims.

## 3. CRAN-style check

```r
rcmdcheck::rcmdcheck(args = "--as-cran")
```
**Gate:** 0 errors / 0 warnings; review NOTEs (expect the usual new-submission /
compiled-shared-lib notes; none should be substantive).

## 4. Rebuild the benchmark cache (the untested edit)

`data-raw/build_benchmark_cache.R` now loads `dsuzukii` (per-individual, 1407 rows)
and aggregates it to `(temp, time, sex)` cells before the count comparator. That
block has not been run. On a toolchain with `bayesTLS` 1.0.0 + CmdStan installed,
re-run the script and confirm the dsuzukii row of
`inst/extdata/bayesTLS_benchmark_cache.rds` is sensible (per-sex CTmax ~35.2 C,
z ~3.0/3.2). Commit the cache only if it changes.

## 5. Close the loop

- Append a `docs/dev-log/check-log.md` entry for the post-remediation commits (the
  0.1.0 unification → audit remediation → Rose/Pat batch) with the `--as-cran`
  result. Rose flagged that the per-slice ledger was not kept in lockstep.
- If §1 showed any man drift, record which files so we know the hand-sync coverage
  gap.

## What's open after this

1. **Retire profileTLS** — superseded `lifecycle` badge + README banner + a gentle
   `.onAttach` pointing to freqTLS in `../profileTLS` (v0.3.3 @ `6f963a9`, the fork
   point); archive, do not delete; then the owner makes it private.
2. **Co-author ORCIDs** — DESCRIPTION lists Nakagawa (cre), Arnold, Pottier, Noble
   (senior) but carries no ORCIDs for the three; add once supplied, with consent
   confirmed.
3. **Optional polish** — Pat's deferred items (a "what to try next" block per case
   study; a model-math orienting paragraph) and Rose's remaining safeguard (a
   prose-vs-data row-count tripwire).
